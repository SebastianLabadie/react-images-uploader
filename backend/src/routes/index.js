const { Router } = require("express");

const router = Router();

router.route("/")
    .get((req, res) => {
    
    });

router.route("/upload")
    .post((req, res) => {

    });

router.route("/image/:id")
    .get((req, res) => {

    })
    .delete((req,res)=>{

    })
